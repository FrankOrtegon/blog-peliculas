import userMiddleware from '../middleware/user';

describe('middleware test functions', () => {
    describe('login user flow test', () => {
        const [loginUserFlow] = userMiddleware;

        const dummyUser = {

        }
        const dummyCount ={

        }
    })
})